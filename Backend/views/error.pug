extends layout 
//- extends layout → use the layout.pug file as the base template.
//- block content → defines (or overrides) the block content section inside that layout.
block content 
h1=title 
p Welcome to #{title}